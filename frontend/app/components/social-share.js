import Ember from 'ember';

export default Ember.Component.extend({
  tagName: 'div',
  classNames: ["ui", "inverted", "labeled", "icon", "left", "fixed", "vertical", "menu", "social-share"],
  // didInsertElement: function() {
  //
  // }.on('didInsertElement')
});
